import TitleContent from "@/ui/components/TitleContent";

export default function ProviderContent(): React.ReactNode {
  return (
    <div>
      <TitleContent title="Providers" description="Manage your providers" />
    </div>
  );
}
